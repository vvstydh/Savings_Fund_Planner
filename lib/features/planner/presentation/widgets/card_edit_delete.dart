import 'package:flutter/material.dart';

class CardEditDelete extends StatelessWidget {
  const CardEditDelete({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      child: Column(
        children: [ElevatedButton(onPressed: () {}, child: Text('knk'))],
      ),
    );
  }
}
